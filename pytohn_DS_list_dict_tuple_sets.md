```python
# my_list = [1, 2, 3, "hello", True]
# print(my_list)
# print(type(my_list))


# my_list.append(4)    # add element at the end
# my_list.insert(1, "new") # add element at specific index
# my_list.remove("hello") # Remove specific element
# my_list.pop()   # remove and return the last element 
# my_list.sort()  # sort the list (if all the elements are comparable)
# my_list.reverse() # Reverse the list
# print(len(my_list)) # get the length of the list


##### Dict ######


# my_dict = {"name": "Shiv", "age": 25, "is_student": True}


# print(my_dict["name"])  # Access value by key 
# my_dict["city"] = "Patna" # Add or update a key-value pair
# my_dict.pop("age")  # Remove a key-value pair
# print(my_dict.keys())  # Get all the keys
# print(my_dict.values()) # Get all the values
# print(my_dict.items()) # Get all the key-values pairs as tuples


#### Tuples #########



my_tuple = (1, 2, 3, "hello", True)

print(my_tuple[0])    # access by index
print(my_tuple.count(2)) # Count occurance of an element 
print(my_tuple.index("hello")) # find the index of an element


#### Set ####

my_set = {1, 2, 3, 4, 4, 5, 5} # Duplicates are removed

my_set.add(5)  # add an element
my_set.remove(3) # Remove an element
union_set = my_set.union({6, 7}) # Union of sets
intersect_set = my_set.intersection({2, 4, 6}) # intersection of sets





```
